import request from '@/utils/request'

export default {
  // uploadUrl: `${request.defaults.baseURL}/base/files/upload`, //上传
  // getDicts: (params) => request.get('/base/sysDict', params), //获取所有字典
  // getDictByCode: (code) => request.get(`/base/sysDictValue/list/${code}`), // 根据 code 获取字典列表
  // getCurrentUser: (params) => request.get('/user/account/current', params), //获取企业类型为运营企业的列表
  // getRoleAll: (params) => request.get('/user/role/all', params), //获取角色类型下拉列表
  // getDeptUserTree: (params) => request.get('/workflow/departments/userTree', params), // 获取组织用户树
  // getRoleUserTree: (params) => request.get('/workflow/roles/users', params), // 获取角色用户树
  // getRoomTree: (communityId) => request.get(`/asset/asset/room/getRoomTree/${communityId}`), // 下拉房间树
}
